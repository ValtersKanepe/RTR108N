\documentclass{beamer}
 
\usepackage[utf8]{inputenc}
\usepackage{tikz}
\usetikzlibrary{calc}
\usetikzlibrary{decorations.pathmorphing}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{fancyhdr}
\usepackage{geometry}
\usepackage[landscape]{geometry}
\usepackage[footskip=115mm]{geometry}
\usepackage{pdfpages}



\begin{document}


    \centering
\begin{tikzpicture}[overlay,remember picture]
    \draw [line width=3pt,rounded corners=24pt,
        ]
        ($ (current page.north west) + (0.2cm,-0.5cm) $)
        rectangle
        ($ (current page.south east) + (-0.2cm,0.8cm) $);
\end{tikzpicture}


\newline\newline\newline
\newline
\newline
    Applications of Stochastic Differential Equations (SDE)
\begin{itemize}
  \item Modeling with SDE: Ito vs. Stratonovich (6.1 \& 6.2)
  \item Parameter Estimation (6.4)
  \item Optimal Stochastic Control (6.5)
  \item Filtering (6.6)
  \end{itemize}
  
\pagestyle{plain}



\newpage
\begin{tikzpicture}[overlay,remember picture]
    \draw [line width=3pt,rounded corners=24pt,
        ]
        ($ (current page.north west) + (0.2cm,-0.5cm) $)
        rectangle
        ($ (current page.south east) + (-0.2cm,0.8cm) $);
\end{tikzpicture}
\newline\newline
        with consistency conditions such as
\begin{equation}
	\resetWhitespace
	\lim_{n\to\infty} NE(X^{(N)}_{k+1} - X^{(N)}_{k}\mid X^{(N)}_{k} = x) = \lim_{N\to\infty}E(a_{N}(x)) = a(x)
	\notag
\end{equation}        
\begin{equation}
	\resetWhitespace
	\lim_{n\to\infty} NE((X^{(N)}_{k+1} - X^{(N)}_{k})^2\mid X^{(N)}_{k} = x) = \lim_{N\to\infty}E(b^2_{N}(x)) = b^2(x)
	\notag
\end{equation}  
\begin{equation}
	\resetWhitespace
	\lim_{n\to\infty} NE(\midX^{(N)}_{k+1} - X^{(N)}_{k})\mid^3\mid X^{(N)}_{k} = x) = 0
	\notag
\end{equation} 
\begin{underrightarrow{N\to\infty}} \end{underrightarrow}  to Ito stochastic differential equation
\begin{equation}
	\resetWhitespace
	dX_{t} = a(X_{t})dt + b(X_{t})dW_{t}
	\notag
\end{equation}
\setcounter{page}{6} 
\newpage
\begin{tikzpicture}[overlay,remember picture]
    \draw [line width=3pt,rounded corners=24pt,
        ]
        ($ (current page.north west) + (0.2cm,-0.5cm) $)
        rectangle
        ($ (current page.south east) + (-0.2cm,0.7cm) $);
\end{tikzpicture}
\newline
\begin{itemize}
  \item the Hamilton-Jacobi-Bellman (HJB) equation
\end{itemize}
  The minimum cost functional
  \begin{equation}
	\resetWhitespace
	H(s,x) = \min\limits_{u(\cdot)}J(s,x,u(\cdot))
	\notag
\end{equation} 
The HJB equation
\begin{equation}
	\resetWhitespace
\min\limits_{u\in\Re^k}\{F(s,x,u) + L_{u}H(s,x)\} = 0
	\notag
\end{equation} 

with the final time condition H(T,x) = K(T,x)

where $\tau$ = T and
\begin{equation}
	\resetWhitespace
        L_{u} = \frac{\partial}{\partial s} + \sum_{i=1}^{d} a^{i} (s,x,u)\frac{\partial}{\partial x_{i}} + \frac{1}{2} \sum_{i,j=1}^{d} D^{i,j}(s,x,u) \frac{\partial^2}{\partial x_[i\partial x_{j}}
	\notag
\end{equation} 
with D = $bb\top$
\pagestyle{plain}
\setcounter{page}{11} 

\end{document}